using AutoMapper;
using Moq;
using TvSeriesApi.Data;
using TvSeriesApi.Data.Entities;
using TvSeriesApi.DTO.Episode;
using TvSeriesApi.Services;

namespace TvSeriesApi.Tests
{
    public class EpisodesServiceTests
    {
        //private readonly EpisodeService _serviceTested;
        //private readonly Mock<IUnitOfWork> _episodeUnitOfWorkMock = new();
        //private readonly Mock<IMapper> _mapperMock = new();
        
        //public EpisodesServiceTests()
        //{
        //    _serviceTested = new EpisodeService(_episodeUnitOfWorkMock.Object, _mapperMock.Object);
        //}

        //// linijka 45
        //[Test]
        //public async Task GetByIdAsync_ShouldReturnEpisode_WhenEpisodeExists()
        //{
        //    // Arrange
        //    var episodeId = 1;
        //    var seasonId = 1;
        //    var episodeName = "White Lines";
        //    var episodeSeason = "Season 1";
        //    var episode = new Episode
        //    {
        //        EpisodeId = episodeId,
        //        Name = episodeName,
        //        SeasonId = seasonId,
        //        Season = episodeSeason
        //    };

        //    var episodeReadDto = new EpisodeReadDTO
        //    {
        //        Name = episodeName,
        //        Season = episodeSeason
        //    };

        //    _episodeUnitOfWorkMock.Setup(x => x.Episodes.GetEpisodeWithSeasonAsync(episodeId))
        //        .ReturnsAsync();

        //    // Act
        //    var episode = await _serviceTested.GetEpisodeByIdAsync(episodeId);
            
        //    // Assert
        //    Assert.Equals(episodeId, episode.i)
            
        //}

        
        
    }
}